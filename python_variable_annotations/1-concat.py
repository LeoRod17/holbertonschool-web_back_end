#!/usr/bin/env python3
"""Making an add Strings function"""


def concat(str1: str, str2: str) -> str:
    """a function that takes a string str1 and a string str2 as
    arguments and returns a concatenated string"""
    ret = str1 + str2
    return ret
